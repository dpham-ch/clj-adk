# clojure_base_template
My own default clojure template in order to built new stuff.

## Dependencies

This library depends on the `@google/generative-ai` npm package when used in ClojureScript.
Please ensure you install it:

```bash
npm install
```
