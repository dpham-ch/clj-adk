(ns clj-adk.events
  (:import [com.google.adk.events Event]))

;; Event is mostly a data holder or created by the system.
;; I'll expose a helper if needed, or just leave it as a placeholder for now.
